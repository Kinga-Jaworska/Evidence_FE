import type { NextPage } from "next";
import { AddForm } from "../src/components/forms";

const Home: NextPage = () => {
  return (
    <div>
      <AddForm />
    </div>
  );
};

export default Home;
